//this component contains the non member functions

import React from "react";

export function NotMemberDash (props) {
    return (
        <>
            <p>Welcome {props.address}</p>
            
        </>
    );
}